# Plan pointer: hard-gate-hdsp-rules

This is a pointer file only, so `subagent-driven-development`'s
`scripts/sdd-workspace` / `scripts/review-package` (which take a single
PLAN_FILE path and derive the workspace directory from its basename) resolve
to the correct existing workspace directory:

`.superpowers/sdd/2026-09-02-hard-gate-hdsp-rules/`

The actual plan is that directory's `progress.md` (ledger) plus its six
`task-N-brief.md` files (written directly during planning, matching this
project's own pre-existing SDD folder convention rather than a single
monolithic plan file).
